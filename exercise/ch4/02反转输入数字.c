/* 
  ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ 这里使用了取模运算符（%）和整除运算符（/）来获取变量n的个位、十位和百位数字。                                                                           │
  │                                                                                                                    │                                                                                                                                                                                                       │
  │ 注意事项：                                                                                                              │
  │                                                                                                                    │
  │ 代码没有进行输入验证，如果用户输入的不是三位数，程序可能会出错。                                                                                   │
  │                                                                                                                    │
  │ 代码没有进行异常处理，如果用户输入的不是数字，程序可能会出错。                                                                                    │
  │                                                                                                                    │
  │ 代码没有进行格式化输出，如果用户输入的数字不是三位数，输出结果可能不正确。                                                                              │
  │                                                                                                                    │
  │ 代码没有进行错误处理，如果用户输入的不是数字，程序可能会崩溃。                                                                                    │
  │                                                                                                                    │
  └────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
 */
#include <stdio.h>

int main(void)
{
  int n;

  printf("Enter a three-digit number: ");
  //scanf("%d", &n);
// 读取用户输入的整数，并将其存储在变量n中
    // !=1 限制整数
    // n<100 限制三位数
    // n>999 限制四位数
    if (scanf("%d", &n) != 1 || n < 100 || n > 999) {
        printf("输入错误，请输入一个三位数。\n");
        return 1; // 返回1表示程序异常结束
    }

  // 取末位、中间位、高位
  printf("The reversal is: %d%d%d\n", n % 10, (n / 10) % 10, n / 100);
  return 0;
}
